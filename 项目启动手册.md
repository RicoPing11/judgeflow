# JudgeFlow Demo 项目启动手册

日期：2026-08-13  
适用范围：JudgeFlow 仓库根目录

本文只说明整个 JudgeFlow 比赛 Demo 的启动、检查、恢复和重新部署。阶段五的功能成果与验收证据见[阶段五 AgentTeams 验收报告](验收成果/阶段-5-AgentTeams/验收报告.md)。

## 1. 什么叫“稳定启动”

本项目运行时只有三个核心容器：

| 容器 | 作用 | 镜像 | 持久化 |
|---|---|---|---|
| `judgeflow-stage4-postgres` | 八张业务表、WorkOrder、Artifact、规则与回放数据 | `postgres:16-alpine` | PostgreSQL Docker volume |
| `judgeflow-agentteams` | AgentTeams Controller、Matrix、MinIO、Higress、Element | `agentteams-embedded:v1.2.0` | `judgeflow-agentteams-data:/data` |
| `judgeflow-mcp` | JudgeFlow 五个 MCP 工具的 HTTP transport | `judgeflow-mcp:stage5-v2` | 无业务数据 |

Python 业务内核运行在 MCP 容器内。阶段六比赛演示时，另在宿主机启动一个标准库 HTTP 进程提供单页和最小后端接口；它不新增容器、框架、数据库或状态机，日常环境恢复时不要求常驻。Element 已包含在 AgentTeams 基础容器中。

本手册所说的“稳定启动”是指：保留现有 Docker volumes 和 AgentTeams 资源时，Docker Engine 重启后，三个核心容器会自动拉起；所有健康门通过后，系统可以继续运行固定 Demo 链路。

它不保证删除数据卷、换机器或重新安装 AgentTeams 后无需迁移即可恢复。数据卷丢失会改变 Matrix 房间、内部凭据和资源状态，必须按第 7 节重新部署。

稳定性依靠以下约束：

1. 三个核心容器的 restart policy 均为 `unless-stopped`。
2. 不删除 PostgreSQL 和 `judgeflow-agentteams-data` 数据卷。
3. 以真实健康端点判断就绪，不使用固定 sleep 代替健康检查。
4. Worker 固定使用 `judgeflow-copaw-worker:stage5-v2`，MCP 固定使用 `judgeflow-mcp:stage5-v2`。
5. 同一 Worker 的模型任务串行；不同 Worker 可以并行。
6. 以数据库中的 WorkOrder、Artifact 和聚合状态判断业务成功，不能只看 HTTP 200 或聊天回复。

## 2. 日常启动顺序

通常只需启动 Docker Desktop/Engine。三个容器会因 `unless-stopped` 自动恢复。需要手动启动时，按以下顺序执行。

### 2.1 PostgreSQL

```bash
docker start judgeflow-stage4-postgres
docker exec judgeflow-stage4-postgres pg_isready -U judgeflow -d judgeflow
```

必须看到 `accepting connections`。

### 2.2 AgentTeams、Matrix、MinIO 与 Higress

```bash
docker start judgeflow-agentteams

curl -i http://127.0.0.1:18090/healthz
curl -i http://127.0.0.1:18080/_matrix/client/versions
curl -i http://127.0.0.1:18088/

docker exec judgeflow-agentteams curl -i http://127.0.0.1:9000/minio/health/live
docker exec judgeflow-agentteams curl -i http://127.0.0.1:8080/health
```

成功条件：

| 组件 | 成功条件 |
|---|---|
| Controller | `http://127.0.0.1:18090/healthz` 返回 HTTP 200，正文 `ok` |
| Matrix | `/_matrix/client/versions` 返回 HTTP 200 |
| Element | `http://127.0.0.1:18088/` 返回 HTTP 200 |
| MinIO | 容器内 `/minio/health/live` 返回 HTTP 200 |
| Higress | 容器内 `/health` 返回 HTTP 200 |

受控冷启动实测中，Controller 会先恢复，Matrix 在约 8 秒时仍可能返回 502，约 20 秒后才恢复 200。Controller 200 不代表 Matrix 已经能收发消息，必须单独等待 Matrix 门通过。

### 2.3 JudgeFlow MCP

```bash
docker start judgeflow-mcp
docker exec judgeflow-mcp curl -i http://127.0.0.1:8765/healthz
```

成功正文应为：

```json
{"ok":true,"transport":"streamable-http","tools":5}
```

MCP 刚重启时可能短暂连接拒绝，实测约 3 秒后恢复。MCP 不映射宿主 `8765` 端口，只允许 Higress 通过 `judgeflow-agentteams-net` 和别名 `judgeflow-mcp.internal` 访问。

### 2.4 阶段六单页 Demo

前三项健康门和第 4 节资源检查通过后，在仓库根目录运行：

```bash
DATABASE_URL='<PRIVATE_DATABASE_URL>' \
UV_CACHE_DIR=/tmp/judgeflow-uv-cache \
uv run python -m app.demo.server --host 127.0.0.1 --port 8086
```

浏览器访问：

```text
http://127.0.0.1:8086/
```

页面进程直接复用现有 Python 项目、PostgreSQL、Kernel、五个 MCP 工具契约和确定性回放器。页面本身只调用 `/api/demo/*`，不直连数据库、Agent、Matrix 或 MCP。停止页面使用 `Ctrl-C`；不要增加守护脚本或额外容器。

页面使用 `DEMO-STAGE6-...` 隔离 ID。重复运行、重复审批和刷新是读取既有正式结果，不重复创建 Artifact。需要新演示时填写新的隔离 ID，不清理或覆盖旧运行，更不能清理阶段五数据。

当前 HTTP 服务提供 `frontend/index.html`、`frontend/app.css`、`frontend/refinement.css` 和 `frontend/app.js`。[frontend/product-concept.html](frontend/product-concept.html) 只作为已确认的设计参考，不是运行入口，其中静态模拟数据不会进入当前页面。

运行页面使用同一 Python 服务提供的只读投影接口：

- `GET /api/demo/overview`；
- `GET /api/demo/cases`、`GET /api/demo/cases/{case_id}`；
- `GET /api/demo/policies`、`GET /api/demo/policies/{policy_id}`；
- `GET /api/demo/approvals`；
- `GET /api/demo/replays/{replay_run_id}`。

这些查询复用现有数据库模型，不新增表，也不会在刷新页面时创建 WorkOrder、Artifact 或推进业务状态。原有阶段六写接口保持不变；具体字段和剩余数据缺口见[产品化页面开发交接](docs/JudgeFlow-产品化页面开发交接.md)。

## 3. 冷启动后的 default Manager

当前 `agentteams-embedded:v1.2.0` 每次冷启动都会额外创建一个内置 `default` Manager。该行为已经连续两次重启复现，不是偶发残留。

Matrix 返回 200 后执行：

```bash
docker exec judgeflow-agentteams agt get managers
docker exec judgeflow-agentteams agt delete manager default
```

删除是异步的，约 5 秒后再次执行：

```bash
docker exec judgeflow-agentteams agt get managers
```

最终只能保留：

```text
judgeflow-manager  Running  deepseek-v4-flash  copaw
```

不要增加循环删除的守护脚本。比赛 Demo 启动前使用官方 CLI 删除一次并检查即可。

## 4. 启动后的资源检查

```bash
docker exec judgeflow-agentteams agt get managers
docker exec judgeflow-agentteams agt get workers
docker exec judgeflow-agentteams agt get teams
docker exec judgeflow-agentteams agt get humans
```

必须满足：

- 只有 `judgeflow-manager`，状态 Running，模型 `deepseek-v4-flash`；
- 10 个 Worker 全部存在，模型均为 `deepseek-v4-flash`，平时状态为 Sleeping；
- `judgeflow-adjudication` 与 `judgeflow-policy` 都是 Active；
- `policy-owner` 是 Active；
- Team 的 `READY=0/n` 在成员都 Sleeping 时属于正常状态；
- Worker 镜像是 `judgeflow-copaw-worker:stage5-v2`；
- MCP 镜像是 `judgeflow-mcp:stage5-v2`。

抽样检查 Worker 的模型和单并发设置：

```bash
docker exec judgeflow-agentteams agt worker ensure-ready --name case-investigator

docker exec agentteams-worker-case-investigator \
  curl -sS http://127.0.0.1:8088/api/models/active

docker exec agentteams-worker-case-investigator sh -c \
  "tr '\000' '\n' < /proc/1/environ | grep -qx 'LLM_MAX_CONCURRENT=1' && echo OK"

docker exec judgeflow-agentteams agt worker sleep --name case-investigator
```

模型必须是 `deepseek-v4-flash`，并发检查必须输出 `OK`。禁止输出完整 `.Config.Env` 或完整 `/proc/1/environ`，避免泄露 API Key 和内部凭据。

## 5. 模型配置

仓库只保存无密钥模板 `agentteams/deepseek.env.example`。私有配置需要以下变量：

```text
AGENTTEAMS_OPENAI_BASE_URL
AGENTTEAMS_LLM_PROVIDER
AGENTTEAMS_LLM_API_KEY
AGENTTEAMS_DEFAULT_MODEL
AGENTTEAMS_MANAGER_MODEL
```

当前模型统一为 `deepseek-v4-flash`。API Key 不得提交、写入本文或打印到共享终端。

模型调用失败时：

1. 先看固定 HTTP 错误码，401/403 通常表示 Key 或授权问题；
2. 检查 Worker 的 `/api/models/active` 是否为 `deepseek-v4-flash`；
3. 更新 AgentTeams 的私有模型配置并重启基础容器；
4. 再按第 2 至第 4 节检查，不要通过打印全部环境变量排障。

2026-08-13 一次诊断命令曾把当前 DeepSeek Key 输出到工具记录。值没有写入仓库，但该 Key 必须在 DeepSeek 后台轮换后再用于演示。

## 6. 镜像重建

日常启动不需要重建镜像。只有 Docker image 丢失或代码发生对应修改时才执行本节。

### 6.1 Worker 镜像

```bash
cd <JUDGEFLOW_REPO>
docker build \
  -f agentteams/copaw-worker-matrix.Dockerfile \
  -t judgeflow-copaw-worker:stage5-v2 \
  agentteams
```

该镜像把 CoPaw 工作目录指回 AgentTeams 官方同步目录，并设置 `LLM_MAX_CONCURRENT=1`。如果镜像内容发生变化，必须使用新 tag，例如 `stage5-v3`，并同步更新 10 个 Worker YAML；不要覆盖旧 tag 后期待 Controller 自动换 digest。

### 6.2 MCP 镜像

```bash
cd <JUDGEFLOW_REPO>
docker build \
  -f agentteams/mcp.Dockerfile \
  -t judgeflow-mcp:stage5-v2 \
  .
```

若 MCP 容器丢失，使用私有配置中的数据库 URL 创建：

```bash
docker run -d \
  --name judgeflow-mcp \
  --restart unless-stopped \
  --network judgeflow-agentteams-net \
  --network-alias judgeflow-mcp.internal \
  -e DATABASE_URL='<DATABASE_URL>' \
  judgeflow-mcp:stage5-v2
```

不要添加 `-p 8765:8765`。`<DATABASE_URL>` 只能来自私有配置，不能把真实密码写进仓库或文档。

## 7. 新机器或数据卷丢失后的重新部署

这是重新部署，不是普通启动。

1. 使用 AgentTeams 官方安装器重新安装基础环境并配置模型：

   ```bash
   bash <(curl -sSL https://higress.ai/hiclaw/install.sh)
   ```

2. 不复制旧安装中的内部 token；让官方安装器生成新凭据。
3. 按第 6 节构建 Worker 与 MCP 镜像。
4. 复制并应用仓库资源：

   ```bash
   docker cp agentteams/resources/. judgeflow-agentteams:/tmp/judgeflow-resources/

   docker exec judgeflow-agentteams agt apply -f /tmp/judgeflow-resources/manager.yaml
   docker exec judgeflow-agentteams agt apply -f /tmp/judgeflow-resources/human-policy-owner.yaml

   docker exec judgeflow-agentteams sh -lc \
     'for f in /tmp/judgeflow-resources/workers/*.yaml; do agt apply -f "$f" || exit 1; done'

   docker exec judgeflow-agentteams agt apply -f /tmp/judgeflow-resources/team-adjudication.yaml
   docker exec judgeflow-agentteams agt apply -f /tmp/judgeflow-resources/team-policy.yaml
   ```

5. 安装四个已有 Skill ZIP：

   | Skill ZIP | Worker |
   |---|---|
   | `case-investigation.zip` | `case-investigator` |
   | `case-deliberation.zip` | `risk-prosecutor`、`counter-reviewer`、`independent-judge`、`appeal-reviewer` |
   | `policy-evolution.zip` | `case-attributor`、`policy-author` |
   | `case-replay.zip` | `replay-analyst` |

   ```bash
   docker cp agentteams/packages/. judgeflow-agentteams:/tmp/judgeflow-packages/

   docker exec judgeflow-agentteams agt apply worker --name case-investigator --zip /tmp/judgeflow-packages/case-investigation.zip
   docker exec judgeflow-agentteams agt apply worker --name risk-prosecutor --zip /tmp/judgeflow-packages/case-deliberation.zip
   docker exec judgeflow-agentteams agt apply worker --name counter-reviewer --zip /tmp/judgeflow-packages/case-deliberation.zip
   docker exec judgeflow-agentteams agt apply worker --name independent-judge --zip /tmp/judgeflow-packages/case-deliberation.zip
   docker exec judgeflow-agentteams agt apply worker --name appeal-reviewer --zip /tmp/judgeflow-packages/case-deliberation.zip
   docker exec judgeflow-agentteams agt apply worker --name case-attributor --zip /tmp/judgeflow-packages/policy-evolution.zip
   docker exec judgeflow-agentteams agt apply worker --name policy-author --zip /tmp/judgeflow-packages/policy-evolution.zip
   docker exec judgeflow-agentteams agt apply worker --name replay-analyst --zip /tmp/judgeflow-packages/case-replay.zip
   ```

6. ZIP 安装后再次应用 `agentteams/resources/workers/*.yaml`，以资源声明固定提示、镜像、MCP URL 和 Sleeping 状态。
7. 重建 Higress 的 `mcp-judgeflow` direct-proxy，并只授权八个领域 Worker；Manager 和 Team Leader 不授权。
8. 重新创建并核验 Matrix 三个业务房间。新房间 ID 会变化，必须同步修改 Manager/Leader 提示和 `fixtures/agentteams/rooms.json`。
9. 完成第 2 至第 4 节全部健康与资源检查。

## 8. 业务与回归检查

检查三次首次裁决：

```bash
docker exec judgeflow-stage4-postgres psql -U judgeflow -d judgeflow -P pager=off -c \
"select c.case_id,c.status,count(distinct w.work_order_id) as work_orders,
 count(distinct w.work_order_id) filter (where w.status='SUCCEEDED') as succeeded,
 count(distinct a.artifact_id) as artifacts
 from cases c
 left join work_orders w on w.aggregate_type='CASE' and w.aggregate_id=c.case_id
 left join artifacts a on a.aggregate_type='CASE' and a.aggregate_id=c.case_id
 where c.case_id in ('CASE-STAGE5-001','CASE-STAGE5-002','CASE-STAGE5-003')
 group by c.case_id,c.status order by c.case_id;"
```

三行都必须为 `DECIDED / 4 / 4 / 4`。另外必须满足：

- `APPEAL-STAGE5-001` 为 `DECIDED`，结果为 `OVERTURN`；
- `EVOLUTION-STAGE5-001` 为 `APPROVED`；
- `APPEAL-STAGE5-002` 为 `DECIDED`，结果为 `OVERTURN`；
- `EVOLUTION-STAGE5-002` 为 `CLOSED/EVIDENCE_GAP`，且不存在草案、回放和 Human 下游；
- `MINOR_DANGEROUS_ACT:1.0` 仍为 `BASELINE`。

完整回归：

```bash
TEST_DATABASE_URL='<PRIVATE_TEST_DATABASE_URL>' \
UV_CACHE_DIR=/tmp/judgeflow-uv-cache \
uv run pytest -q
```

截至 2026-08-16，使用现有 PostgreSQL 测试库执行上述命令的当前基线是
`143 passed, 1 warning`。不设置 `TEST_DATABASE_URL` 时，两项 PostgreSQL 在线测试会被明确
标记为 skipped；这不能替代 Demo 前的完整回归。测试失败不能当作 warning 忽略。

## 9. 常见故障

| 现象 | 原因/判断 | 恢复方法 |
|---|---|---|
| Controller 200，Matrix 502 | Matrix 仍在冷启动 | 继续检查 `/_matrix/client/versions`，直到 200 后再发任务 |
| MCP 刚重启连接拒绝 | MCP 尚未监听 | 稍后重试容器内 `/healthz`，不要重建容器 |
| 出现 `default` Manager | embedded v1.2.0 自动创建 | Matrix 200 后删除 `default`，确认只剩正式 Manager |
| Worker Running，但不消费 Matrix | Worker 镜像或 CoPaw 工作目录不正确 | 检查镜像 tag，重新 apply Worker，再执行 Sleeping→Running |
| HTTP 200，但 WorkOrder 仍 PENDING | Agent 对话结束但正式 Artifact 未提交 | 以数据库为准；清理残留会话后用新 session 串行重试 |
| 同一 Worker 两条任务互相拖住 | 同 Worker 模型会话并发 | 确认 `LLM_MAX_CONCURRENT=1`，同 Worker 串行发送 |
| Artifact envelope/hash 错误 | 模型尝试手工构造机械字段 | 只提交紧凑领域 payload，由 MCP 绑定 envelope/hash |
| Team `READY=0/n` | Worker 都处于 Sleeping | 正常；Team 只需 Active，任务前唤醒目标 Worker |
| 房间或成员关系丢失 | AgentTeams 数据卷被删除或重装 | 重新建房并更新资源提示与 rooms fixture |
| 模型 401/403 | Key、Base URL 或模型授权错误 | 更新私有模型配置，轮换 Key，检查 active model；禁止打印 Key |

## 10. 新窗口如何接手

新 Codex 或终端窗口不需要重新安装、重新建库或重建房间。接手顺序：

1. 阅读本文和[阶段五 AgentTeams 验收报告](验收成果/阶段-5-AgentTeams/验收报告.md)；若接手产品化页面，再完整阅读[产品化页面开发交接](docs/JudgeFlow-产品化页面开发交接.md)和[产品开发样本](frontend/product-concept.html)；
2. 执行第 2 节健康检查；
3. 删除冷启动产生的 `default` Manager；
4. 执行第 4 节资源检查；
5. 需要核对历史结果时执行第 8 节；
6. 只有镜像或数据确实丢失时才进入第 6、7 节。

不要在新窗口里因为“不确定”就重新安装 AgentTeams 或删除 Docker volume。

## 11. Demo 前五分钟检查

1. Docker Engine Running；三个核心容器均 Up，restart policy 均为 `unless-stopped`。
2. PostgreSQL、Controller、Matrix、Element、MinIO、Higress、MCP 全部通过健康门。
3. 删除冷启动生成的 `default` Manager，只保留 `judgeflow-manager`。
4. 10 个 Worker 存在且 Sleeping；两个 Team Active；Human Active。
5. Worker 与 MCP 分别使用 `stage5-v2`；active model 是 `deepseek-v4-flash`。
6. DeepSeek Key 已轮换，私有配置未进入仓库或共享输出。
7. 同一 Worker 不并发发两条任务；不同 Worker 可以并行。
8. 业务结果以数据库状态为准，不以聊天正文或 HTTP 200 为准。
9. 按第 8 节连接测试数据库执行完整回归，结果应为 `143 passed, 1 warning`；若测试集合发生有意修改，应先核对收集数量和新增测试依据，再更新本文基线。
10. 演示结束后让领域 Worker 回到 Sleeping；Manager 保持 Running，Team 保持 Active。
11. 按第 2.4 节启动页面，访问 `http://127.0.0.1:8086/`；首次使用新 `DEMO-STAGE6-...` ID，复讲可刷新既有 ID。
