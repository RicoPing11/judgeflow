# JudgeFlow ：自进化的多智能体风险研判与决策治理系统

## 作品简介

高风险内容治理常混淆证据、推断与规则，单一模型既调查又裁决，结论难复核，申诉也难转化为规则改进。JudgeFlow 构建“调查—风险与反证—独立裁决”和“申诉—归因—规则草案—回放—人工审批”两条闭环。

系统以 WorkOrder 驱动 11 个 Agent 资源分工。AgentTeams 与 Matrix 只路由 ID；Agent 通过 5 个 MCP 工具领取授权材料、提交结构化 Artifact，后端负责验收、状态和审计。案件锁定规则快照，正反意见隔离，申诉只读冻结原案和白名单证据；规则作者看不到评分集，回放指标由 Python 计算，Human APPROVE 不发布生产规则。

项目把职责隔离、失败语义、幂等验收和确定性回放组合为可追溯闭环，可迁移到风控、合规、理赔等场景。现已完成 8 张表、4 个 Skill、单页 Demo 和 143 项测试，演示实测 6 分 39 秒。

## 核心 Skill

Skill 是跨 Agent、跨运行复用的任务能力抽象，不是一次性行为描述。

| Skill / 用途 | 输入 → 输出 | 调用条件 | 工具 | 失败与安全边界 | 复用及流程关系 |
|---|---|---|---|---|---|
| `case-investigation`：调查取证 | 授权案件、证据 → `EVIDENCE_BUNDLE` | 调查 WorkOrder | `work_order.get`、`context.get`、`evidence.search`、`artifact.put` | 失败不等于无数据；禁止越权、判断违规、混淆事实与推断 | 为正反研判提供证据，可复用到各类调查 |
| `case-deliberation`：证据—规则审查 | 规则、证据、意见 → 风险/反证/裁决/申诉 Artifact | 调查后正反并行，汇合后裁决；申诉用冻结快照 | `work_order.get`、`context.get`、`artifact.put` | 证据不足明确输出；角色隔离，申诉限白名单 | 四种模式跨角色复用，连接调查、裁决和申诉 |
| `policy-evolution`：归因与草案 | 改判、基线、案例 → 归因/提案 | 改判后归因；仅规则问题起草 | `work_order.get`、`context.get`、`artifact.put` | 非规则问题关闭；禁止读取 `SCORING`、覆盖或发布规则 | 门控申诉与回放，可复用于策略迭代 |
| `case-replay`：确定性回放 | 基线、候选、数据集 → `REPLAY_REPORT` | 提案验收并绑定数据集 | `work_order.get`、`context.get`、`replay.execute`、`artifact.put` | 失败不生成报告；Agent 不修改指标、标签和推荐 | 独立约束人工审批，可复用于规则回归 |

每个 Skill 均提供 `SKILL.md` 及成功、缺失信息、工具失败三类样例，并打包为 AgentTeams Worker 可同步的 ZIP。

## MCP、RAG 与可观测

### MCP

FastMCP 提供 `work_order.get`、`context.get`、`evidence.search`、`artifact.put`、`replay.execute` 五个工具。

| 契约项 | 实现说明 |
|---|---|
| 协议和调用入口 | AgentTeams 经 Higress 使用 Streamable HTTP `/mcp` 调用 FastMCP Server |
| 鉴权方式 | Higress 注入可信身份；服务端复核 Agent、assignee、`run_id`、`trace_id` |
| 参数 Schema 和返回结构 | FastMCP 输入 Schema；统一返回 `{ok,data,error,audit_ref}`；正式产物使用 `ArtifactEnvelope` |
| 权限范围 | 只读 WorkOrder 授权引用；工具按 Agent 白名单开放；不提供状态迁移、处罚、审批或规则发布 |
| 错误处理、失败重试和降级 | 区分未找到、数据不全、越权、校验失败、超时和冲突；失败不当作无数据，关键失败停止状态推进，部分证据可带失败记录提交 |
| 幂等控制 | Artifact 相同幂等键与内容返回既有结果，冲突内容拒绝；写操作成功提交、失败回滚，重试使用新 `run_id` |
| 审计记录 | 返回 `audit_ref`；WorkOrder、Artifact、失败、重试、状态迁移和审批以 `trace_id` 写入 PostgreSQL |

### 为什么不使用 RAG

JudgeFlow 不应该使用 RAG。高风险裁决需要读取 WorkOrder 明确授权的证据、冻结快照和精确规则版本，而不是从相似内容中概率召回。RAG 会引入召回遗漏、越权混入、版本漂移和评分集泄露，还可能把“未召回”误判为“没有数据”，破坏申诉隔离、审计追踪和结果复现。因此系统通过 MCP 按 ID 精确读取结构化材料，并用哈希校验规则、证据和回放数据集；即使未来连接企业知识库，也应使用受控的精确查询接口，而不让语义检索参与正式裁决依据。

### 可观测

JudgeFlow 已实现面向高风险决策的业务可观测闭环：

| 类型 | 采集与存储 | 使用效果 |
|---|---|---|
| Trace | `trace_id` 贯穿 WorkOrder、Artifact、状态事件和审批，`run_id` 区分每次执行与重试；数据存入 PostgreSQL | 从最终裁决反查具体 Agent、任务、输入引用、产物和审批 |
| 结构化事件 | Kernel 记录状态迁移、产物验收、WorkOrder 失败/重试和 Human 审批，事件使用固定类型和错误码 | 解释流程为何推进、停止或重试，区分工具失败、未开始和业务无数据 |
| 产物血缘与完整性 | 保存 `work_order_id`、`artifact_id`、producer、规则快照哈希、Artifact 内容哈希和数据集 manifest 哈希 | 证明结论使用了哪份证据、哪版规则和哪次运行，发现内容或版本不一致 |
| 质量 Metrics | Python 回放器记录基线/候选规则的 FP、FN、变化案件、逐案结果及 `PASS/FAIL/INCONCLUSIVE` | 在 Human 审批前量化候选规则影响，避免由 LLM 自行计算指标 |
| 页面观测 | 单页展示 Agent/WorkOrder 状态，并可下钻查看 `run_id`、`trace_id`、Artifact、失败码和回放详情 | 支持现场演示、问题定位、审计复核和幂等验证 |

这些能力已经覆盖官方建议的 Trace、结构化 Log 和业务质量 Metrics。当前尚未接入 OpenTelemetry Span，也没有可靠的 TTFT、Token、费用等模型运行指标，因此页面明确显示“—”，不使用虚构数据。

## 评审维度亮点

| 维度 | 可强调内容 |
|---|---|
| 场景价值与复制性 | 解决证据混杂、单模型自审和规则无法闭环；替换 Schema、数据适配器、规则 DSL 和数据集即可迁移 |
| 多 Agent 协同 | 职责隔离、正反并行、裁决汇合、非规则分支剪枝、失败停留、Human 把关 |
| Skill 工程体系 | 4 个 Skill 有契约、工具边界和三类样例，输出由 Schema 二次验收 |
| 工程与安全审计 | 真实跑通 Matrix、MCP、入库、申诉、回放和审批；关键对象有哈希与 Trace 绑定 |
| 开放 / 复用 | 提供中英文文档、JSON Schema、fixtures、Skill 样例和测试；未授予开源许可证，不宣称已开源 |

## 一句话差异化

JudgeFlow 不是让多个 Agent 自由讨论，而是用 WorkOrder、最小 MCP 权限、结构化 Artifact、不可变规则快照和确定性回放，把高风险 AI 决策变成可反证、可复核、可追责且有人类把关的工程闭环。
