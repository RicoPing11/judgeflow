# JudgeFlow 比赛 Demo 仓库约束

## 唯一目标

本仓库只用于 GOAI Agent Infra 比赛 Demo。只证明以下闭环可以真实运行：

```text
调查取证 → 风险研判与反证审查 → 独立裁决
申诉改判 → 问题归因 → 规则草案 → 案件回放 → 人工审批
```

所有设计和实现都必须服务于 5–8 分钟的固定演示。不能以“以后可能需要”为理由增加组件。

## 明确不做

- 不建设通用审核平台、生产处罚系统、完整人工审核平台或多行业产品；
- 不增加生产级安全守卫或额外安全中间层；
- 不实现自定义 `curl guard`、`Matrix guard`、`Higress guard`；
- 不增加运行时包装器、启动器套壳或资源守护脚本；
- 不引入额外微服务、RAG、向量数据库、消息队列、服务注册中心或额外数据库；
- 不为高可用、生产扩缩容、生产级权限中心和全链路可观测性做预留；
- 不复制旧项目代码、旧迁移、旧脚本、旧镜像、阶段记录和缓存。

若想新增 Agent、房间、表、MCP 工具、Skill、服务或框架，必须先说明主 Demo 缺少它为何无法完成，并获得用户批准。

## 必须保留的基本边界

- 不执行真实内容处罚或用户权益变更；
- 不发布或替换任何生产规则；Demo 中的批准只记录比赛审批结果；
- 不读取、写入、打印或提交真实密钥、令牌和用户隐私；
- Agent 不直接写数据库或推进业务状态，正式产物必须结构化并由后端验收；
- 工具失败不能解释成查询结果为空；
- 不覆盖用户已有改动，不对旧项目做任何修改。

## 实现原则

- 从 Schema 开始，先跑通固定 Demo 数据，再接 AgentTeams 和页面；
- 选择最简单、可验证的实现，不做通用抽象；
- 后端、MCP 和确定性回放优先放在同一个 Python 项目中；
- Matrix 只传任务 ID、业务 ID、产物引用和简短状态；
- LLM 不计算回放指标，固定 Python 代码负责计算；
- “法律”按不可变版本存储，修改生成候选版本，Human 审批不等于生产发布。

## 文档和验证

- 文档默认使用中文，程序字段和资源名保留英文；
- 不为阶段汇报新增 Markdown 文档；
- 每次只实现当前 Demo 步骤并运行直接相关的最小测试；
- 无法验证的部分必须在交付时明确说明。
