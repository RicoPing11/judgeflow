# JudgeFlow 比赛 Demo 开发顺序

> 本文件只服务于 5–8 分钟比赛 Demo。一次只开发一个阶段；每阶段验收后停止，获得用户批准再进入下一阶段。

## 1. 固定主链路

```text
调查取证 → 风险研判 + 反证审查 → 独立裁决
申诉改判 → 问题归因 → 规则草案 → 确定性回放 → Human 审批
```

任何新增内容都必须能指出它支撑主链路的哪一步。不能回答就不开发。

## 2. 每阶段统一规则

1. 开始前只确认当前阶段范围和验收标准；
2. 只修改当前阶段直接需要的文件；
3. 运行直接相关的最小测试；
4. 把验收报告和必要测试结果保存到 `验收成果/阶段-N-名称/`；
5. 汇报完成内容、测试结果、遗留问题；
6. 停止，等待用户批准；
7. 未批准不得提前进入下一阶段。

验收成果只是运行证据，不是业务真源。不得保存密钥、隐私、完整敏感内容或无关日志。

## 3. 阶段 1：Schema 与固定样例

### 实现

- 初始化最小 Python 3.12 项目；
- 定义 WorkOrder、Artifact Envelope；
- 定义证据包、正方意见、反方意见、裁决、申诉决定、归因报告、规则草案、回放报告和 Human 审批 Schema；
- 定义不可执行任意代码的 Policy DSL 和 Policy Snapshot；
- 准备一个主案件、一次申诉、一个基线规则和最小回放样例；
- 导出 JSON Schema。

### 验收

- 每个 Schema 有成功和非法样例；
- 事实、推断、缺失信息和工具失败明确分开；
- 结论必须引用 `evidence_id` 和规则要件；
- Policy DSL 只允许批准的有限运算符；
- pytest 通过。

### 本阶段不做

数据库、FastAPI、MCP、Agent、AgentTeams、前端和部署。

## 4. 阶段 2：数据库、状态和规则执行

### 实现

- 建立技术设计中的 8 张表和一份初始迁移；
- 实现 Case、Appeal、PolicyEvolution 三个最小状态流；
- 实现 WorkOrder 创建、验收、失败和重试；
- 实现不可变 Artifact、Policy 版本和案件 Policy Snapshot；
- 实现有限 Policy DSL 的确定性执行器；
- 实现固定数据集的新旧规则回放。

### 验收

- 不启动 Agent，也能用测试完整推进主链路；
- 错误 `from_state` 被拒绝；
- 重复提交幂等，重跑生成新 `run_id` 和 Artifact；
- 原案件始终引用锁定的规则版本；
- 相同输入的规则执行和回放结果完全一致；
- 空库迁移成功。

### 本阶段不做

MCP、Skill、Agent、Matrix、页面和安全守卫。

## 5. 阶段 3：五个 MCP 工具与四个 Skill

### 实现

- MCP：`work_order.get`、`context.get`、`evidence.search`、`artifact.put`、`replay.execute`；
- Skill：`case-investigation`、`case-deliberation`、`policy-evolution`、`case-replay`；
- 外部内容、转写、账号和关系数据全部使用固定 fixture；
- 后端、MCP 和回放器仍属于同一个 Python 项目。

### 验收

- MCP 校验 Agent 身份、WorkOrder、输入引用和期望产物类型；
- 申诉只能读取原案快照和新增证据白名单；
- 规则 Agent 不能读取 `SCORING`；
- 工具失败、数据不存在和数据不完整可以区分；
- 每个 Skill 有成功、缺失信息和工具失败样例；
- 不存在状态迁移、生产发布或审批 MCP 工具。

### 本阶段不做

AgentTeams 资源、Matrix 自动化、前端和任何 guard。

## 6. 阶段 4：八个领域 Agent

### 实现

- 调查取证、风险研判、反证审查、独立裁决；
- 申诉重审；
- 案件归因、规则编写、回放分析；
- 每个 Agent 只包含最小身份、边界、Skill 和 MCP 权限；
- 先用本地测试驱动 Agent 输入输出，不接 Matrix。

### 验收

- 首次裁决链路连续运行 3 次，输出结构合法；
- 风险和反证互相独立；
- 申诉不读取原 Team 聊天或白名单外证据；
- 非规则问题可以在归因后关闭；
- 回放指标来自 Python，不由 LLM 计算；
- Agent 不能直接写数据库或推进状态。

### 本阶段不做

生产级 Prompt 平台、记忆系统、RAG、运行时包装器和安全守卫。

## 7. 阶段 5：AgentTeams 与三个房间

### 实现

- 定义一个 Manager、两个 Team Leader、八个领域 Agent 和一个规则负责人 Human；
- 创建总控、案件裁决、规则演进三个业务房间；
- 通过 `@mention` 和 ID 消息唤醒 Agent；
- 非当前步骤 Agent 使用 `Sleeping`；
- Matrix 只传 WorkOrder ID、业务 ID、Artifact ID 和简短状态。

### 验收

- Manager 和 Leader 不生成领域 Artifact；
- 后端验收 Artifact 后才推进业务状态；
- Matrix 自由文本不能作为下游正式输入；
- 峰值四个活跃 Agent 可以完成一次裁决；
- 不包含自定义 curl、Matrix、Higress guard 或运行时包装器。

阶段五完成后的实际启动、健康检查和 AgentTeams 资源恢复统一见[项目启动手册](项目启动手册.md)；本节和阶段验收报告不重复保存运行手册。

### 本阶段不做

生产级资源治理、高可用、压测平台和额外可观测系统。

## 8. 阶段 6：单页 Demo 与最终验收

### 实现

- 单页展示固定案件、当前 Agent、证据、正反意见和原裁决；
- 展示申诉改判、问题归因、规则 Diff、回放指标和 Human 审批；
- 提供固定种子数据和明确启动入口；新演示使用新的 `demo_run_id`，重复 ID 保持幂等且不清理历史结果；
- 完成 README 启动说明和 Demo 讲解顺序。

阶段六实现启动入口时，应同步更新[项目启动手册](项目启动手册.md)，并以该文档作为运行与恢复的唯一详细说明。

### 验收

- 从空库可启动并导入固定数据；
- 完整闭环连续运行 3 次；
- 5–8 分钟可演示完毕；
- 页面不直连 Agent、Matrix 或数据库；
- `APPROVE` 只记录 Demo 审批，不发布生产规则；
- 不需要手工修改数据库；
- 没有旧项目代码、额外基础设施或安全守卫混入。

### 阶段六后的产品化展示改版（P0/P1 已完成）

阶段六真实业务闭环和当前单页已经验收完成；后续改版只提升比赛展示，不扩展业务范围。

- 页面设计参考：[frontend/product-concept.html](frontend/product-concept.html)；
- 开发交接入口：[docs/JudgeFlow-产品化页面开发交接.md](docs/JudgeFlow-产品化页面开发交接.md)；
- 当前运行页面是 `frontend/index.html`、`frontend/app.css`、`frontend/refinement.css`、`frontend/app.js`；
- 运行监控、案件列表与详情、规则列表与详情、待审批列表与详情、回放详情已经通过最小只读投影接口接入；
- 页面查询复用现有数据模型，不新增表；刷新不会创建 WorkOrder、Artifact 或推进业务状态；
- 产品样本中的批量数字、Token、响应时间和预设时间线没有进入运行页面；
- Token、费用、裁决置信度、规则条件级命中/例外次数和回放业务时间范围等缺少可靠来源的指标继续显示“—”或“暂无数据”；
- 不得把样本中的静态模拟状态、计数或交互冒充真实 Agent 运行。

后续维护仍不得新增 Agent、房间、表、MCP、Skill、服务、框架或状态中心；只在可靠数据源到位或发现明确缺陷时做最小增量，并运行直接相关的测试。

## 9. 验收成果目录约定

阶段完成后再创建对应子目录：

```text
验收成果/
├── 阶段-1-Schema/
├── 阶段-2-业务内核/
├── 阶段-3-MCP与Skill/
├── 阶段-4-领域Agent/
├── 阶段-5-AgentTeams/
└── 阶段-6-最终Demo/
```

每个阶段只保存：

- `验收报告.md`：范围、结果、问题和结论；
- 必要测试输出或截图；
- 无法运行项目时的原因和未验证范围。

不要保存重复设计文档、长篇开发日志、缓存、模型原始对话、密钥或敏感数据。
